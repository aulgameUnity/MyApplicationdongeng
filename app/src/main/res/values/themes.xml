<resources>
<!-- Base application theme. -->
<style name="Theme.CeritaDongeng" parent="Theme.MaterialComponents.Light.NoActionBar">
    <item name="colorPrimary">@color/pink</item>
    <item name="colorPrimaryVariant">@color/pink</item>
    <item name="colorOnPrimary">@color/white</item>
    <item name="colorSecondary">@color/pink</item>
    <item name="colorSecondaryVariant">@color/pink</item>
    <item name="colorOnSecondary">@color/black</item>
    <item name="android:statusBarColor">@color/pink</item>
    <item name="android:fontFamily">@font/next_sunday</item>
</style>

    <!-- Use ThemeOverlay as parent when using with android:theme to avoid infinite cycles.
         Inheriting from Widget.AppCompat.SearchView while using it as a theme
         causes circular lookups for attributes like textAppearance. -->
<style name="SearchView" parent="ThemeOverlay.AppCompat.Dark">
<item name="android:editTextColor">@android:color/white</item>
<item name="android:textColorHint">@android:color/white</item>
<item name="android:textStyle">bold</item>
</style>
</resources>